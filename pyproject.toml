[project]
name = "skinstinct-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "google-genai>=0.3.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "api.telegram_webhook:handler"
